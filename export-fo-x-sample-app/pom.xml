<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>com.pixeldweller.export</groupId>
    <artifactId>export-fo-x-parent</artifactId>
    <version>1.0.0-SNAPSHOT</version>
  </parent>

  <artifactId>export-fo-x-sample-app</artifactId>
  <!-- Property-driven so one module can build either artefact; see the war profile. -->
  <packaging>${sample.app.packaging}</packaging>

  <properties>
    <sample.app.packaging>jar</sample.app.packaging>
    <!-- jar: Tomcat is embedded. war: the container provides it. -->
    <tomcat.scope>compile</tomcat.scope>
  </properties>

  <name>export-fo-x-sample-app</name>
  <description>Spring-Boot-Beispiel: nimmt ein DOCX entgegen und antwortet mit PDF.</description>

  <dependencyManagement>
    <dependencies>
      <dependency>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-dependencies</artifactId>
        <version>${spring.boot.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
    </dependencies>
  </dependencyManagement>

  <dependencies>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-web</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-tomcat</artifactId>
      <scope>${tomcat.scope}</scope>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-validation</artifactId>
    </dependency>

    <dependency>
      <groupId>com.pixeldweller.export</groupId>
      <artifactId>export-fo-x</artifactId>
    </dependency>

    <!-- JAXB-Implementierung fuer docx4j (zur Laufzeit erforderlich) -->
    <dependency>
      <groupId>org.docx4j</groupId>
      <artifactId>docx4j-JAXB-ReferenceImpl</artifactId>
      <version>${docx4j.version}</version>
    </dependency>

    <!-- docx4j/FOP loggen ueber commons-logging bzw. JCL -> auf SLF4J bruecken -->
    <dependency>
      <groupId>org.slf4j</groupId>
      <artifactId>jcl-over-slf4j</artifactId>
      <version>${slf4j.version}</version>
    </dependency>

    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-test</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.apache.pdfbox</groupId>
      <artifactId>pdfbox</artifactId>
      <version>3.0.6</version>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <profiles>
    <!--
      mvn -Pwar package  ->  target/export-fo-x-sample-app-<version>.war

      Deployable in an external Tomcat. ExportFoXSampleApplication already extends
      SpringBootServletInitializer, so no source changes are needed; note that main()
      is NOT called in this mode, which is why the startup work sits in a static
      initialiser there.
    -->
    <profile>
      <id>war</id>
      <properties>
        <sample.app.packaging>war</sample.app.packaging>
        <tomcat.scope>provided</tomcat.scope>
      </properties>
      <build>
        <plugins>
          <plugin>
            <groupId>org.apache.maven.plugins</groupId>
            <artifactId>maven-war-plugin</artifactId>
            <version>3.4.0</version>
            <configuration>
              <!-- A Spring Boot war needs no web.xml and no src/main/webapp. -->
              <failOnMissingWebXml>false</failOnMissingWebXml>
            </configuration>
          </plugin>
        </plugins>
      </build>
    </profile>
  </profiles>

  <build>
    <resources>
      <resource>
        <directory>src/main/resources</directory>
      </resource>
      <!-- Apache-2.0 §4(a)/§4(d): Lizenz und NOTICE reisen mit dem Artefakt mit. -->
      <resource>
        <directory>${project.basedir}/..</directory>
        <targetPath>META-INF</targetPath>
        <includes>
          <include>LICENSE</include>
          <include>NOTICE</include>
        </includes>
      </resource>
    </resources>
    <plugins>
      <plugin>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-maven-plugin</artifactId>
        <version>${spring.boot.version}</version>
        <executions>
          <execution>
            <goals><goal>repackage</goal></goals>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-compiler-plugin</artifactId>
      </plugin>
    </plugins>
  </build>
</project>
